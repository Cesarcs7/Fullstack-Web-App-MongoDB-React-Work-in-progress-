import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

const FirstWaschstrasse = (props) => {
    return (
        <div className="firsWaschstrasseForm">
            <form action="">
            <input type="text"/>
            </form>
        </div>
    );
};

export default FirstWaschstrasse;
